FROM dpage/pgadmin4
USER root

ARG NETWORK
ARG PROJ_NAME
ARG PGADMIN_DEFAULT_EMAIL
ARG PGADMIN_DEFAULT_PASSWORD
ARG POSTGRES_DB
ARG POSTGRES_PORT
ARG POSTGRES_USER
ARG POSTGRES_PASSWORD

COPY install_pgpass.sh /
RUN chmod +x /install_pgpass.sh
RUN /install_pgpass.sh

