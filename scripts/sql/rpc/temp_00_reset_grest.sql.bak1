-- Recreate koios schema
DROP SCHEMA IF EXISTS koios CASCADE;

-- CREATE SCHEMA koios;

-- GRANT USAGE ON SCHEMA koios TO authenticator, web_anon;

-- GRANT SELECT ON ALL TABLES IN SCHEMA koios TO authenticator, web_anon;

-- ALTER DEFAULT PRIVILEGES IN SCHEMA koios GRANT
-- SELECT ON TABLES TO authenticator, web_anon;
